{
  "parameters": {
    "byok_base_url": "${ customer_secrets.KUMULOS_BASE ?? https://api.kumulos.com }/v2/notifications",
    "byok_headers": {
      "Authorization": "Basic ${ customer_secrets.KUMULOS_BASIC }"
    },
    "channel_name": "kumulos-relay",
    "code_example_preset": "notify_relay_code_example_py_kumulos",
    "connectivity_preset": "notify_relay_connectivity_kumulos",
    "description": "`kumulos-relay` accepts an HTTP POST with a native `kumulos` payload. The gateway composes the upstream Kumulos URL from your stored secrets, injects your credential header, and forwards the request, so your credentials never leave the platform.\n\nTwo channels are available: the free `byok` channel uses your own stored credentials; the `plus` channel binds an extra per-enrollment credential via enrollment parameters (the `api_key_secret` parameter names the customer secret holding this enrollment's base64 of user:password for HTTP basic auth).",
    "display_name": "Kumulos Relay",
    "logo": "https://avatars.githubusercontent.com/u/21124837?v=4&s=200",
    "ops_testing_parameters": {
      "api_key_secret": "KUMULOS_BASIC",
      "base_url": "https://mock.unitysvc.dev/kumulos"
    },
    "plus_base_url": "${ customer_secrets.KUMULOS_BASE ?? https://api.kumulos.com }/v2/notifications",
    "plus_headers": {
      "Authorization": "Basic ${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "summary": "Relay a notification to your own Kumulos by posting a native `kumulos` payload to the UnitySVC gateway.",
    "tags": [
      "notification",
      "kumulos",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/v2/notifications",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's base64 of user:password for HTTP basic auth",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
