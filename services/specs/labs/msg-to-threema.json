{
  "parameters": {
    "apprise_url": "threema://${ customer_secrets.THREEMA_GATEWAY_ID }@${ customer_secrets.THREEMA_SECRET }/${ customer_secrets.THREEMA_TARGETS }",
    "apprise_url_plus": "threema://${ customer_secrets.{{ params.gateway_id_secret }} }@${ customer_secrets.{{ params.secret_secret }} }/${ customer_secrets.{{ params.targets_secret }} }",
    "channel_name": "msg-to-threema",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "default_visibility": "private",
    "description": "`msg-to-threema` accepts an HTTP POST carrying the unified `msg` envelope (`title`, `body`, `type`, `format`), attaches the credentials in your stored secrets, and delivers the message to your Threema destination — so you run no messaging infrastructure of your own.\n\nDelivery goes out through [Apprise](https://github.com/caronc/apprise), the open-source notification library, driven entirely by your own stored credentials at no cost. A paid variant ($0.001 per message) delivers to additional Threema destinations bound per enrollment.\n\nCreate a Threema Gateway account at gateway.threema.ch and confirm your email address, then request a \"Basic\" Simple Gateway ID at gateway.threema.ch/en/id-request?type=simple (approval typically takes one to two business days — encrypted End-to-End IDs are not supported here). Once approved, your 8-character Gateway ID (e.g. `*MYGWYID`) and its ID secret appear on the ID overview page — set these as THREEMA_GATEWAY_ID and THREEMA_SECRET. Set THREEMA_TARGETS to the recipient(s) you want to message: their Threema ID, phone number, or registered email address. You'll also need to top up Gateway credits (ask support-gateway@threema.ch for test credits, or purchase them after logging in) before messages will send.",
    "display_name": "Threema Messages",
    "logo": "https://cdn.simpleicons.org/threema/3FE669",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "gateway_id_secret": "THREEMA_GATEWAY_ID",
      "secret_secret": "THREEMA_SECRET",
      "targets_secret": "THREEMA_TARGETS"
    },
    "summary": "Send a notification to your own Threema destination by posting a simple `msg` request to the UnitySVC gateway.",
    "test_apprise_url": "threema://*ABC1234@threemasecretxyz123/+15551234567",
    "time_created": "2026-06-20T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "gateway_id_secret": {
          "description": "Name of the customer secret holding this enrollment's Threema gateway id",
          "type": "string"
        },
        "secret_secret": {
          "description": "Name of the customer secret holding this enrollment's Threema secret",
          "type": "string"
        },
        "targets_secret": {
          "description": "Name of the customer secret holding this enrollment's Threema targets",
          "type": "string"
        }
      },
      "required": [
        "gateway_id_secret",
        "secret_secret",
        "targets_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
