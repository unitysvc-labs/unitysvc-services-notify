{
  "parameters": {
    "apprise_url": "join://${ customer_secrets.JOIN_APIKEY }/${ customer_secrets.JOIN_TARGETS }",
    "apprise_url_plus": "join://${ customer_secrets.{{ params.apikey_secret }} }/${ customer_secrets.{{ params.targets_secret }} }",
    "channel_name": "msg-to-join",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "description": "`msg-to-join` accepts an HTTP POST carrying the unified `msg` envelope (`title`, `body`, `type`, `format`), attaches the credentials in your stored secrets, and delivers the message to your Join destination — so you run no messaging infrastructure of your own.\n\nDelivery goes out through [Apprise](https://github.com/caronc/apprise), the open-source notification library, driven entirely by your own stored credentials at no cost. A paid variant ($0.001 per message) delivers to additional Join destinations bound per enrollment.",
    "display_name": "Join Messages",
    "logo": "https://raw.githubusercontent.com/joaomgcd/JoinChrome/master/icons/big.png",
    "ops_testing_parameters": {
      "apikey_secret": "JOIN_APIKEY",
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "targets_secret": "JOIN_TARGETS"
    },
    "summary": "Send a notification to your own Join destination by posting a simple `msg` request to the UnitySVC gateway.",
    "tags": [
      "notification",
      "join",
      "apprise",
      "gateway"
    ],
    "test_apprise_url": "join://dkry5cjqx4bipw3ahov29gnu18fmt07e/dkry5cjqx4bipw3ahov29gnu18fmt07e",
    "time_created": "2026-06-20T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "apikey_secret": {
          "description": "Name of the customer secret holding this enrollment's Join apikey",
          "type": "string"
        },
        "targets_secret": {
          "description": "Name of the customer secret holding this enrollment's Join targets",
          "type": "string"
        }
      },
      "required": [
        "apikey_secret",
        "targets_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
