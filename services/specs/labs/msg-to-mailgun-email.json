{
  "parameters": {
    "apprise_url": "mailgun://${ customer_secrets.MAILGUN_EMAIL_USER }@${ customer_secrets.MAILGUN_EMAIL_HOST }/${ customer_secrets.MAILGUN_EMAIL_APIKEY }/",
    "apprise_url_plus": "mailgun://${ customer_secrets.{{ params.user_secret }} }@${ customer_secrets.{{ params.host_secret }} }/${ customer_secrets.{{ params.apikey_secret }} }/",
    "channel_name": "msg-to-mailgun-email",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "description": "`msg-to-mailgun-email` accepts an HTTP POST carrying the unified `msg` envelope (`title`, `body`, `type`, `format`), attaches the credentials in your stored secrets, and delivers the message to your Mailgun account — so you run no messaging infrastructure of your own.\n\nDelivery goes out through [Apprise](https://github.com/caronc/apprise) — the open-source notification library — or a native gateway transformation, both free and driven entirely by your own stored credentials. Paid variants ($0.001 per message) deliver to additional Mailgun accounts bound per enrollment.",
    "display_name": "Mailgun Messages",
    "gateway_body_template": "from=${ customer_secrets.MAILGUN_EMAIL_FROM }&to=${ customer_secrets.MAILGUN_EMAIL_TO }&subject={* _escape_json(_body.title or \"\") *}&text={* _escape_json(_body.body or \"\") *}",
    "gateway_code_example_preset": "msg_to_channel_code_example_py_mailgun_email",
    "gateway_connectivity_preset": "msg_to_channel_connectivity_mailgun_email",
    "gateway_content_type": "application/x-www-form-urlencoded",
    "gateway_extra_headers": {
      "Authorization": "Basic YXBpOm1haWxndW5fZW1haWxhcGlrZXl4eXoxMjM="
    },
    "gateway_plus_price_description": "$0.001 per message — extra per-enrollment Mailgun destinations, direct",
    "gateway_price_description": "Free — direct to your Mailgun sending domain",
    "gateway_test_local_url": "https://mock.unitysvc.dev/mailgun_email/v3/notify.example.com/messages",
    "gateway_url": "${ customer_secrets.MAILGUN_EMAIL_BASE ?? https://api.mailgun.net }/v3/${ customer_secrets.MAILGUN_EMAIL_HOST }/messages",
    "gateway_url_plus": "${ customer_secrets.MAILGUN_EMAIL_BASE ?? https://api.mailgun.net }/v3/${ customer_secrets.{{ params.host_secret }} }/messages",
    "logo": "https://cdn.simpleicons.org/mailgun/F06B66",
    "ops_testing_parameters": {
      "apikey_secret": "MAILGUN_EMAIL_APIKEY",
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "host_secret": "MAILGUN_EMAIL_HOST",
      "user_secret": "MAILGUN_EMAIL_USER"
    },
    "summary": "Send an email notification through your own Mailgun account by posting a simple `msg` request to the UnitySVC gateway.",
    "tags": [
      "notification",
      "mailgun_email",
      "apprise",
      "gateway"
    ],
    "test_apprise_url": "mailgun://mailgun_emailuser@notify.example.com/mailgun_emailapikeyxyz123/",
    "time_created": "2026-06-20T00:00:00Z",
    "transformer_channel_exists": true,
    "user_parameters_schema": {
      "properties": {
        "apikey_secret": {
          "description": "Name of the customer secret holding this enrollment's Mailgun apikey",
          "type": "string"
        },
        "host_secret": {
          "description": "Name of the customer secret holding this enrollment's Mailgun host",
          "type": "string"
        },
        "user_secret": {
          "description": "Name of the customer secret holding this enrollment's Mailgun user",
          "type": "string"
        }
      },
      "required": [
        "user_secret",
        "host_secret",
        "apikey_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
