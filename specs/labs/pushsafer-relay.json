{
  "parameters": {
    "byok_base_url": "${ customer_secrets.PUSHSAFER_BASE ?? https://www.pushsafer.com }/api",
    "channel_name": "pushsafer-relay",
    "code_example_preset": "notify_relay_code_example_py_pushsafer",
    "connectivity_preset": "notify_relay_connectivity_pushsafer",
    "description": "Relay messages to your own Pushsafer through the UnitySVC gateway. POST a native pushsafer payload; the gateway composes the upstream URL from your stored secrets and forwards it. The free byok channel uses your own stored credentials; the plus channel binds extra per-enrollment destinations via parameters.",
    "display_name": "Pushsafer Relay",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/pushsafer"
    },
    "plus_base_url": "${ customer_secrets.PUSHSAFER_BASE ?? https://www.pushsafer.com }/api",
    "tags": [
      "notification",
      "pushsafer",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/api",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
