{
  "parameters": {
    "byok_base_url": "${ customer_secrets.MAILGUN_EMAIL_BASE ?? https://api.mailgun.net }/v3/${ customer_secrets.MAILGUN_EMAIL_HOST }/messages",
    "byok_headers": {
      "Authorization": "Basic ${ customer_secrets.MAILGUN_EMAIL_BASIC }"
    },
    "channel_name": "mailgun_email-relay",
    "code_example_preset": "notify_relay_code_example_py_mailgun_email",
    "connectivity_preset": "notify_relay_connectivity_mailgun_email",
    "description": "Relay messages to your own Mailgun through the UnitySVC gateway. POST a native mailgun_email payload; the gateway composes the upstream URL from your stored secrets, injects your credential header, and forwards it. The free byok channel uses your own stored credentials; the plus channel binds an extra per-enrollment credential via parameters.",
    "display_name": "Mailgun Relay",
    "ops_testing_parameters": {
      "api_key_secret": "MAILGUN_EMAIL_BASIC",
      "base_url": "https://mock.unitysvc.dev/mailgun_email"
    },
    "plus_base_url": "${ customer_secrets.MAILGUN_EMAIL_BASE ?? https://api.mailgun.net }/v3/${ customer_secrets.MAILGUN_EMAIL_HOST }/messages",
    "plus_headers": {
      "Authorization": "Basic ${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "tags": [
      "notification",
      "mailgun",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/v3/demo.example.com/messages",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's base64 of user:password for HTTP basic auth",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
