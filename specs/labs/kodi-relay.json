{
  "parameters": {
    "byok_base_url": "${ customer_secrets.KODI_BASE ?? http://kodi.local:8080 }/jsonrpc",
    "channel_name": "kodi-relay",
    "code_example_preset": "notify_relay_code_example_py_kodi",
    "connectivity_preset": "notify_relay_connectivity_kodi",
    "description": "Relay messages to your own Kodi through the UnitySVC gateway. POST a native kodi payload; the gateway composes the upstream URL from your stored secrets and forwards it. The free byok channel uses your own stored credentials; the plus channel binds extra per-enrollment destinations via parameters.",
    "display_name": "Kodi Relay",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/kodi"
    },
    "plus_base_url": "${ customer_secrets.KODI_BASE ?? http://kodi.local:8080 }/jsonrpc",
    "tags": [
      "notification",
      "kodi",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/jsonrpc",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
