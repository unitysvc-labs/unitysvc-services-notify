{
  "parameters": {
    "byok_base_url": "${ customer_secrets.AFRICASTALKING_SMS_BASE ?? https://api.africastalking.com }/version1/messaging",
    "byok_headers": {
      "apiKey": "${ customer_secrets.AFRICASTALKING_SMS_APIKEY }"
    },
    "channel_name": "africastalking_sms-relay",
    "code_example_preset": "notify_relay_code_example_py_africastalking_sms",
    "connectivity_preset": "notify_relay_connectivity_africastalking_sms",
    "description": "Relay messages to your own Africa's Talking SMS through the UnitySVC gateway. POST a native africastalking_sms payload; the gateway composes the upstream URL from your stored secrets, injects your credential header, and forwards it. The free byok channel uses your own stored credentials; the plus channel binds an extra per-enrollment credential via parameters.",
    "display_name": "Africa's Talking SMS Relay",
    "ops_testing_parameters": {
      "api_key_secret": "AFRICASTALKING_SMS_APIKEY",
      "base_url": "https://mock.unitysvc.dev/africastalking_sms"
    },
    "plus_base_url": "${ customer_secrets.AFRICASTALKING_SMS_BASE ?? https://api.africastalking.com }/version1/messaging",
    "plus_headers": {
      "apiKey": "${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "tags": [
      "notification",
      "africastalking",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/version1/messaging",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's API credential",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
