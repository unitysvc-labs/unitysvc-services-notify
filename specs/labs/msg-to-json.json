{
  "parameters": {
    "apprise_url": "json://${ customer_secrets.JSON_HOST }",
    "apprise_url_plus": "json://${ customer_secrets.{{ params.host_secret }} }",
    "channel_name": "msg-to-json",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "description": "Send notifications to your own JSON Webhook destination through the UnitySVC gateway. POST the unified message envelope {title, body, type, format}; the gateway composes an Apprise json:// URL from your stored secrets and delivers via apprise.unitysvc.dev. The free apprise channel uses your own stored credentials; the apprise-plus channel binds extra per-enrollment destinations via parameters.",
    "display_name": "JSON Webhook Messages",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "host_secret": "JSON_HOST"
    },
    "tags": [
      "notification",
      "json",
      "apprise",
      "gateway"
    ],
    "test_apprise_url": "json://hostname.example.com",
    "time_created": "2026-06-19T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "host_secret": {
          "description": "Name of the customer secret holding this enrollment's JSON Webhook host",
          "type": "string"
        }
      },
      "required": [
        "host_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
