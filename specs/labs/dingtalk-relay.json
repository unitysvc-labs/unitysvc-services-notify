{
  "parameters": {
    "byok_base_url": "${ customer_secrets.DINGTALK_BASE ?? https://oapi.dingtalk.com }/robot/send?access_token=${ customer_secrets.DINGTALK_TOKEN }",
    "channel_name": "dingtalk-relay",
    "code_example_preset": "notify_relay_code_example_py_dingtalk",
    "connectivity_preset": "notify_relay_connectivity_dingtalk",
    "description": "Relay messages to your own DingTalk through the UnitySVC gateway. POST a native dingtalk payload; the gateway composes the upstream URL from your stored secrets and forwards it. The free byok channel uses your own stored credentials; the plus channel binds extra per-enrollment destinations via parameters.",
    "display_name": "DingTalk Relay",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/dingtalk",
      "token_secret": "DINGTALK_TOKEN"
    },
    "plus_base_url": "${ customer_secrets.DINGTALK_BASE ?? https://oapi.dingtalk.com }/robot/send?access_token=${ customer_secrets.{{ params.token_secret }} }",
    "tags": [
      "notification",
      "dingtalk",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/robot/send?access_token=demotoken",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "token_secret": {
          "description": "Name of the customer secret holding this enrollment's dingtalk token",
          "type": "string"
        }
      },
      "required": [
        "token_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
