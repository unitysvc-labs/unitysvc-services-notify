{
  "parameters": {
    "byok_base_url": "${ customer_secrets.VIBER_BASE ?? https://chatapi.viber.com }/pa/send-message",
    "byok_headers": {
      "X-Viber-Auth-Token": "${ customer_secrets.VIBER_TOKEN }"
    },
    "channel_name": "viber-relay",
    "code_example_preset": "notify_relay_code_example_py_viber",
    "connectivity_preset": "notify_relay_connectivity_viber",
    "description": "Relay messages to your own Viber through the UnitySVC gateway. POST a native viber payload; the gateway composes the upstream URL from your stored secrets, injects your credential header, and forwards it. The free byok channel uses your own stored credentials; the plus channel binds an extra per-enrollment credential via parameters.",
    "display_name": "Viber Relay",
    "ops_testing_parameters": {
      "api_key_secret": "VIBER_TOKEN",
      "base_url": "https://mock.unitysvc.dev/viber"
    },
    "plus_base_url": "${ customer_secrets.VIBER_BASE ?? https://chatapi.viber.com }/pa/send-message",
    "plus_headers": {
      "X-Viber-Auth-Token": "${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "tags": [
      "notification",
      "viber",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/pa/send-message",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's API credential",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
