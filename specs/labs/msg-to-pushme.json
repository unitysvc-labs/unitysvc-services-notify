{
  "parameters": {
    "apprise_url": "pushme://${ customer_secrets.PUSHME_TOKEN }",
    "apprise_url_plus": "pushme://${ customer_secrets.{{ params.token_secret }} }",
    "channel_name": "msg-to-pushme",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "description": "Send notifications to your own PushMe destination through the UnitySVC gateway. POST the unified message envelope {title, body, type, format}; the gateway composes an Apprise pushme:// URL from your stored secrets and delivers via apprise.unitysvc.dev. The free apprise channel uses your own stored credentials; the apprise-plus channel binds extra per-enrollment destinations via parameters.",
    "display_name": "PushMe Messages",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "token_secret": "PUSHME_TOKEN"
    },
    "tags": [
      "notification",
      "pushme",
      "apprise",
      "gateway"
    ],
    "test_apprise_url": "pushme://pushmetokenxyz123",
    "time_created": "2026-06-20T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "token_secret": {
          "description": "Name of the customer secret holding this enrollment's PushMe token",
          "type": "string"
        }
      },
      "required": [
        "token_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
