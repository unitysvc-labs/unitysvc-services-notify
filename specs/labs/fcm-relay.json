{
  "parameters": {
    "byok_base_url": "${ customer_secrets.FCM_BASE ?? https://fcm.googleapis.com }/fcm/send",
    "byok_headers": {
      "Authorization": "key=${ customer_secrets.FCM_APIKEY }"
    },
    "channel_name": "fcm-relay",
    "code_example_preset": "notify_relay_code_example_py_fcm",
    "connectivity_preset": "notify_relay_connectivity_fcm",
    "description": "Relay messages to your own FCM (legacy) through the UnitySVC gateway. POST a native fcm payload; the gateway composes the upstream URL from your stored secrets, injects your credential header, and forwards it. The free byok channel uses your own stored credentials; the plus channel binds an extra per-enrollment credential via parameters.",
    "display_name": "FCM (legacy) Relay",
    "ops_testing_parameters": {
      "api_key_secret": "FCM_APIKEY",
      "base_url": "https://mock.unitysvc.dev/fcm"
    },
    "plus_base_url": "${ customer_secrets.FCM_BASE ?? https://fcm.googleapis.com }/fcm/send",
    "plus_headers": {
      "Authorization": "key=${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "tags": [
      "notification",
      "fcm",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/fcm/send",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's API key",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
