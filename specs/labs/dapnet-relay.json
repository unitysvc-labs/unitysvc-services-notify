{
  "parameters": {
    "byok_base_url": "${ customer_secrets.DAPNET_BASE ?? https://www.hampager.de }/api/calls",
    "byok_headers": {
      "Authorization": "Basic ${ customer_secrets.DAPNET_BASIC }"
    },
    "channel_name": "dapnet-relay",
    "code_example_preset": "notify_relay_code_example_py_dapnet",
    "connectivity_preset": "notify_relay_connectivity_dapnet",
    "description": "Relay messages to your own DAPNET through the UnitySVC gateway. POST a native dapnet payload; the gateway composes the upstream URL from your stored secrets, injects your credential header, and forwards it. The free byok channel uses your own stored credentials; the plus channel binds an extra per-enrollment credential via parameters.",
    "display_name": "DAPNET Relay",
    "ops_testing_parameters": {
      "api_key_secret": "DAPNET_BASIC",
      "base_url": "https://mock.unitysvc.dev/dapnet"
    },
    "plus_base_url": "${ customer_secrets.DAPNET_BASE ?? https://www.hampager.de }/api/calls",
    "plus_headers": {
      "Authorization": "Basic ${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "tags": [
      "notification",
      "dapnet",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/api/calls",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's base64 of user:password for HTTP basic auth",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
