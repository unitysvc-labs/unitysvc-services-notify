{
  "parameters": {
    "apprise_url": "victorops://${ customer_secrets.VICTOROPS_ROUTING_KEY }@${ customer_secrets.VICTOROPS_APIKEY }",
    "apprise_url_plus": "victorops://${ customer_secrets.{{ params.routing_key_secret }} }@${ customer_secrets.{{ params.apikey_secret }} }",
    "channel_name": "msg-to-victorops",
    "code_example_preset": "apprise_notify_code_example_py_victorops",
    "connectivity_preset": "apprise_notify_connectivity_victorops",
    "description": "Send notifications to your own Splunk On-Call destination through the UnitySVC gateway. POST the unified message envelope {title, body, type, format}; the gateway composes an Apprise victorops:// URL from your stored secrets and delivers via apprise.unitysvc.dev. The free apprise channel uses your own stored credentials; the apprise-plus channel binds extra per-enrollment destinations via parameters.",
    "display_name": "Splunk On-Call Messages",
    "ops_testing_parameters": {
      "apikey_secret": "VICTOROPS_APIKEY",
      "base_url": "https://mock.unitysvc.dev/notify",
      "routing_key_secret": "VICTOROPS_ROUTING_KEY"
    },
    "tags": [
      "notification",
      "victorops",
      "apprise",
      "gateway"
    ],
    "test_webhook_path": "/notify",
    "time_created": "2026-06-19T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "apikey_secret": {
          "description": "Name of the customer secret holding this enrollment's Splunk On-Call apikey",
          "type": "string"
        },
        "routing_key_secret": {
          "description": "Name of the customer secret holding this enrollment's Splunk On-Call routing key",
          "type": "string"
        }
      },
      "required": [
        "routing_key_secret",
        "apikey_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
