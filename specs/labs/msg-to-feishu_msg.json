{
  "parameters": {
    "apprise_url": "feishu://${ customer_secrets.FEISHU_MSG_TOKEN }",
    "apprise_url_plus": "feishu://${ customer_secrets.{{ params.token_secret }} }",
    "channel_name": "msg-to-feishu_msg",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "description": "Send a notification to your own Feishu destination by posting a simple `msg` request to the UnitySVC gateway.\n\n`msg-to-feishu_msg` accepts an HTTP POST carrying the unified `msg` envelope (`title`, `body`, `type`, `format`). The gateway composes an Apprise `feishu://` URL from the credentials in your stored secrets and delivers the notification via apprise.unitysvc.dev, so you run no messaging infrastructure of your own.\n\nTwo channels are available: the free `apprise` channel uses your own stored credentials; the `apprise-plus` channel binds extra per-enrollment destinations via enrollment parameters.",
    "display_name": "Feishu Messages",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "token_secret": "FEISHU_MSG_TOKEN"
    },
    "tags": [
      "notification",
      "feishu_msg",
      "apprise",
      "gateway"
    ],
    "test_apprise_url": "feishu://demotoken123456",
    "time_created": "2026-06-19T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "token_secret": {
          "description": "Name of the customer secret holding this enrollment's Feishu token",
          "type": "string"
        }
      },
      "required": [
        "token_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
