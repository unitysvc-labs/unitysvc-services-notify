{
  "parameters": {
    "apprise_url": "lark://${ customer_secrets.LARK_TOKEN }",
    "apprise_url_plus": "lark://${ customer_secrets.{{ params.token_secret }} }",
    "channel_name": "msg-to-lark",
    "code_example_preset": "msg_to_apprise_code_example_py",
    "connectivity_preset": "msg_to_apprise_connectivity",
    "description": "Send a notification to your own Lark (Feishu) destination by posting a simple `msg` request to the UnitySVC gateway.\n\n`msg-to-lark` accepts an HTTP POST carrying the unified `msg` envelope (`title`, `body`, `type`, `format`). The gateway composes an Apprise `lark://` URL from the credentials in your stored secrets and delivers the notification via apprise.unitysvc.dev, so you run no messaging infrastructure of your own.\n\nTwo channels are available: the free `apprise` channel uses your own stored credentials; the `apprise-plus` channel binds extra per-enrollment destinations via enrollment parameters (`token_secret`).",
    "display_name": "Lark (Feishu) Messages",
    "ops_testing_parameters": {
      "base_url": "https://mock.unitysvc.dev/apprise/notify",
      "token_secret": "LARK_TOKEN"
    },
    "tags": [
      "notification",
      "lark",
      "apprise",
      "gateway"
    ],
    "test_apprise_url": "lark://dkry5cjqx4bi",
    "time_created": "2026-06-20T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "token_secret": {
          "description": "Name of the customer secret holding this enrollment's Lark (Feishu) token",
          "type": "string"
        }
      },
      "required": [
        "token_secret"
      ],
      "type": "object"
    }
  },
  "template": "msg-to-channel"
}
