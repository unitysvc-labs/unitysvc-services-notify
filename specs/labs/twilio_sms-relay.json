{
  "parameters": {
    "byok_base_url": "${ customer_secrets.TWILIO_SMS_BASE ?? https://api.twilio.com }/2010-04-01/Accounts/${ customer_secrets.TWILIO_SMS_ACCOUNT_SID }/Messages.json",
    "byok_headers": {
      "Authorization": "Basic ${ customer_secrets.TWILIO_SMS_BASIC }"
    },
    "channel_name": "twilio_sms-relay",
    "code_example_preset": "notify_relay_code_example_py_twilio_sms",
    "connectivity_preset": "notify_relay_connectivity_twilio_sms",
    "description": "Relay messages to your own Twilio SMS through the UnitySVC gateway. POST a native twilio_sms payload; the gateway composes the upstream URL from your stored secrets, injects your credential header, and forwards it. The free byok channel uses your own stored credentials; the plus channel binds an extra per-enrollment credential via parameters.",
    "display_name": "Twilio SMS Relay",
    "ops_testing_parameters": {
      "api_key_secret": "TWILIO_SMS_BASIC",
      "base_url": "https://mock.unitysvc.dev/twilio_sms"
    },
    "plus_base_url": "${ customer_secrets.TWILIO_SMS_BASE ?? https://api.twilio.com }/2010-04-01/Accounts/${ customer_secrets.TWILIO_SMS_ACCOUNT_SID }/Messages.json",
    "plus_headers": {
      "Authorization": "Basic ${ customer_secrets.{{ params.api_key_secret }} }"
    },
    "tags": [
      "notification",
      "twilio",
      "webhook",
      "gateway"
    ],
    "test_webhook_path": "/2010-04-01/Accounts/ACdeadbeefdeadbeef/Messages.json",
    "time_created": "2026-06-21T00:00:00Z",
    "user_parameters_schema": {
      "properties": {
        "api_key_secret": {
          "description": "Name of the customer secret holding this enrollment's base64 of user:password for HTTP basic auth",
          "type": "string"
        }
      },
      "required": [
        "api_key_secret"
      ],
      "type": "object"
    }
  },
  "template": "notify-relay"
}
