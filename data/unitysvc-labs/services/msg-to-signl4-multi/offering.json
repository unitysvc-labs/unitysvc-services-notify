{
  "capabilities": [
    "notification"
  ],
  "description": "SMTP-to-SIGNL4 transformer (multi-enrollment). Each enrollment targets a different SIGNL4 endpoint, provided by the customer via named secrets. Receives email through the UnitySVC SMTP gateway, transforms the canonical envelope into a SIGNL4-native message, and delivers it to the customer's own SIGNL4 endpoint.",
  "name": "msg-to-signl4-multi",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "alerting",
    "incident",
    "mobile",
    "notification",
    "ops",
    "signl4",
    "smtp",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "${ customer_secrets.{{ params.webhook_base_url_secret }} }",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"Title\":{* _escape_json(_body.title) *},\"Message\":{* _escape_json(_body.body) *}}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "${ customer_secrets.{{ params.webhook_path_secret }} }"
          }
        }
      }
    }
  }
}
