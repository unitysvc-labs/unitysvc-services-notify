{
  "capabilities": [
    "notification"
  ],
  "description": "SMTP-to-SimplePush transformer. Receives email through the UnitySVC SMTP gateway, extracts title/body/from, and reshapes the payload into a SimplePush-native message before forwarding to the pre-configured webhook. No customer secrets required \u2014 the webhook URL is registered by the service operator.",
  "name": "msg-to-simplepush",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "notification",
    "push",
    "simplepush",
    "smtp",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "${ secrets.SIMPLEPUSH_BASE_URL ?? http://localhost:8100/mock/simplepush }",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"msg\":{* _escape_json(_body.title..\": \".._body.body) *},\"event\":{* _escape_json(_body.title) *}}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "/send/{key}"
          }
        }
      }
    }
  }
}
