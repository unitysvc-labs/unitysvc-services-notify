{
  "capabilities": [
    "notification"
  ],
  "description": "SMTP-to-Telegram transformer (multi-enrollment). Each enrollment targets a different Telegram webhook URL, provided by the customer. Receives email through the UnitySVC SMTP gateway, transforms the canonical envelope into a Telegram-native message, and delivers it to the customer's own Telegram webhook.",
  "name": "msg-to-telegram-multi",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "chat",
    "messaging",
    "notification",
    "smtp",
    "telegram",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "{{ enrollment_vars.webhook_base_url }}",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"chat_id\":{* _escape_json(enrollment_vars.chat_id) *},\"text\":{* _escape_json(_body.title..\"\n\n\".._body.body) *}}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "{{ enrollment_vars.webhook_path }}"
          }
        }
      }
    }
  }
}
