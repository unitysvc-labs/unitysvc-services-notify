{
  "capabilities": [
    "notification"
  ],
  "description": "HTTP-to-Matrix transformer. Accepts canonical notification payloads (title/body/from) via HTTP POST and reshapes the payload into a Matrix-native message before forwarding to the pre-configured webhook. No customer secrets required \u2014 the webhook URL is registered by the service operator.",
  "name": "msg-to-matrix",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "chat",
    "http",
    "matrix",
    "notification",
    "open-protocol",
    "self-hosted",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "${ secrets.MATRIX_BASE_URL ?? https://mock.unitysvc.dev/matrix/_matrix/client/v3/rooms/!test:localhost/send/m.room.message/1 }",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"msgtype\":\"m.text\",\"body\":{* _escape_json(_body.title..\"\n\n\".._body.body) *}}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "/_matrix/client/v3/rooms/{room}/send/m.room.message/{txn}"
          }
        }
      }
    }
  }
}
