{
  "capabilities": [
    "notification",
    "messaging"
  ],
  "description": "Multi-enrollment IRC notification relay. Each enrollment connects a different IRC account. Customers provide the secret name as a parameter, enabling multiple configurations under a single account.",
  "name": "notify-irc-relay",
  "payout_price": {
    "description": "Pricing per notification sent",
    "percentage": "100.00",
    "type": "revenue_share"
  },
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "notify",
    "irc",
    "relay",
    "messaging"
  ],
  "time_created": "2026-04-29T00:00:00Z",
  "upstream_access_config": {
    "Notify Worker": {
      "access_method": "http",
      "api_key": "${ customer_secrets.{{ params.password_secret }} ?? }",
      "base_url": "${NOTIFY_WORKER_BASE_URL}",
      "routing_key": {
        "channel": "irc",
        "server": "{{ params.server }}",
        "nick": "{{ params.nick }}",
        "irc_channel": "{{ params.irc_channel }}"
      }
    }
  }
}
