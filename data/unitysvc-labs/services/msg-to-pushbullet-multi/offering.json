{
  "capabilities": [
    "notification"
  ],
  "description": "HTTP-to-Pushbullet transformer (multi-enrollment). Each enrollment targets a different Pushbullet endpoint, provided by the customer via named secrets. Accepts canonical notification payloads (title/body/from) via HTTP POST and transforms them into a Pushbullet-native message, and delivers it to the customer's own Pushbullet endpoint.",
  "name": "msg-to-pushbullet-multi",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "http",
    "mobile",
    "notification",
    "push",
    "pushbullet",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "${ secrets.PUSHBULLET_BASE_URL ?? http://localhost:8100/mock/pushbullet }",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"type\":\"note\",\"title\":{* _escape_json(_body.title) *},\"body\":{* _escape_json(_body.body) *}}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Access-Token": "${ customer_secrets.{{ params.api_key_secret }} ?? test_key }",
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "/v2/pushes"
          }
        }
      }
    }
  }
}
