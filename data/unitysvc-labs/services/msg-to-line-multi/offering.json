{
  "capabilities": [
    "notification"
  ],
  "description": "SMTP-to-LINE transformer (multi-enrollment). Each enrollment targets a different LINE endpoint, provided by the customer via named secrets. Receives email through the UnitySVC SMTP gateway, transforms the canonical envelope into a LINE-native message, and delivers it to the customer's own LINE endpoint.",
  "name": "msg-to-line-multi",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "asia",
    "line",
    "messaging",
    "notification",
    "smtp",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "https://api.line.me",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"to\":${ customer_secrets.{{ params.to_id_secret }} },\"messages\":[{\"type\":\"text\",\"text\":{* _escape_json(_body.title..\"\\n\\n\".._body.body) *}}]}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Authorization": "Bearer ${ customer_secrets.{{ params.api_key_secret }} }",
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "/v2/bot/message/push"
          }
        }
      }
    }
  }
}
