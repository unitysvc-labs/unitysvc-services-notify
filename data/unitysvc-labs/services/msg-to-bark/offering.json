{
  "capabilities": [
    "notification"
  ],
  "description": "SMTP-to-Bark transformer. Receives email through the UnitySVC SMTP gateway, extracts title/body/from, and reshapes the payload into a Bark-native message before forwarding to the pre-configured webhook. No customer secrets required \u2014 the webhook URL is registered by the service operator.",
  "name": "msg-to-bark",
  "schema": "offering_v1",
  "service_type": "notification",
  "status": "ready",
  "tags": [
    "bark",
    "ios",
    "notification",
    "push",
    "smtp",
    "transformer"
  ],
  "time_created": "2026-06-01T00:00:00Z",
  "upstream_access_config": {
    "webhook": {
      "access_method": "http",
      "base_url": "${ secrets.BARK_BASE_URL ?? http://localhost:8100/mock/bark }",
      "is_active": true,
      "is_primary": true,
      "raw": {
        "request_transformer": {
          "body_transformer": {
            "request": {
              "input_format": "json",
              "template": "{\"title\":{* _escape_json(_body.title) *},\"body\":{* _escape_json(_body.body) *}}"
            }
          },
          "proxy_rewrite": {
            "headers": {
              "set": {
                "Content-Type": "application/json"
              }
            },
            "method": "POST",
            "uri": "/{device_key}"
          }
        }
      }
    }
  }
}
